# Project group
1. James Railton
2. Billy Ryan
3. Eloise Edwards
4. Tomi
5. Adlan

# Preparation
This project was build using R-Studio version 4.4.2 
The project use several R packages for data processing includes:`tidyverse`, `dplyr`, `stringr`, `readr`, `tidyr`, `lubridate`, `ggplot2`, `ggrepel` ,`randomForest`, `e1071`, `xgboost`, `pROC`, `DMwR`, `ROSE`, `caret` `pROC`, `caret` 
The project content for reading is in the report folder when reading labeled being the enumeration of the subfolder. 
